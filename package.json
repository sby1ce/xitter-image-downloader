{
  "name": "xitter-image-downloader",
  "private": true,
  "version": "0.6.0",
  "type": "module",
  "scripts": {
    "compile": "tsc --noEmit",
    "dev": "vite",
    "build": "bun run build.ts",
    "build:firefox": "node --experimental-strip-types build.ts firefox",
    "check": "biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.4",
    "@types/webextension-polyfill": "^0.12.3"
  },
  "dependencies": {
    "lightningcss": "^1.30.1",
    "sass": "^1.92.1",
    "typescript": "^5.9.2",
    "vite": "npm:rolldown-vite@^7.1.9",
    "vite-plugin-web-extension": "^4.4.5",
    "webextension-polyfill": "^0.12.0"
  }
}
